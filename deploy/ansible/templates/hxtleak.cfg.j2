[server]
debug_mode = 1
http_port  = 80
http_addr  = 0.0.0.0
static_path = {{ hxt_install_dir }}/static
adapters   = hxtleak
enable_cors = true

[tornado]
logging = info

[adapter.hxtleak]
module = hxtleak.adapter.HxtleakAdapter
port_name = /dev/ttyS1
